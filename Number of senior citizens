class Solution {
    public int countSeniors(String[] details) {
        int result = 0;

        // iterate each string data in he details array
        for(String data : details)
        {
            int age =Integer. parseInt(data.substring(11,13));

            if(age > 60)
            {
                result++;
            }
        }
        return result;



    }

}
